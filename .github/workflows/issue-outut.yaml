name: Output Issue Details
on:
    issues:
        types:
        - opened
jobs:
    Fetch-show-details:
        runs-on: ubuntu-latest
        steps:
            - name: Issue details
              run: echo "${{ toJson(github.event.issue) }}"